#include<stdio.h>
 using namespace std;
 main(){
 	//sizeof确认占用内存空间
	 long long n=100;
	 printf("%d\n",sizeof (long long)) ;
	short m=100;
	 printf("%d\n",sizeof (short)) ;
	int l=100;
	 printf("%d\n",sizeof (int)) ; 
	//使用确认数组长度 
	//int len = sizeof (数组名) / sizeof(数组[0]); 
	
	
 }
